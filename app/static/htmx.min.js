// HTMX is intentionally served locally (no CDN at runtime).
// The production Docker image fetches a pinned HTMX build at build time.
// This stub keeps local runs from failing hard if you haven't vendored HTMX yet.
window.htmx = window.htmx || {};
